import { iChannel } from "../types";

export class Suspilne_Krym implements iChannel {

    name = 'Суспільне Крим' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}