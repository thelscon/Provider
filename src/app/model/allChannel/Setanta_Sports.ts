import { iChannel } from "../types";

export class Setanta_Sports implements iChannel {

    name = 'Setanta Sports' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}