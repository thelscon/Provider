import { iChannel } from "../types";

export class Animal_Planet implements iChannel {

    name = 'Animal Planet' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}