import { iChannel } from "../types";

export class Paramount_Comedy implements iChannel {

    name = 'Paramount Comedy' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}