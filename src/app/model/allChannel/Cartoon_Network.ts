import { iChannel } from "../types";

export class Cartoon_Network implements iChannel {

    name = 'Cartoon Network' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}