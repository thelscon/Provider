import { iChannel } from "../types";

export class FilmBox_ArtHouse implements iChannel {

    name = 'FilmBox ArtHouse' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}