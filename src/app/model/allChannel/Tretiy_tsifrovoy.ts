import { iChannel } from "../types";

export class Tretiy_tsifrovoy implements iChannel {

    name = 'Третий цифровой' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}