import { iChannel } from "../types";

export class Pervyy_gorodskoy implements iChannel {

    name = 'Первый городской' ;
    broadcastFormat = {
        analog : true ,
        digital : true
    }
    
}